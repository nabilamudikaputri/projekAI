# Customize Color Scheme

  This is a code bundle for Customize Color Scheme. The original project is available at https://www.figma.com/design/fLJ55N4ymdxOdU9sPzcwCy/Customize-Color-Scheme.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development serv